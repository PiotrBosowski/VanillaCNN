#include "MatrixToMatrix.h"
#include <iostream>

void MatrixToMatrix::connect(std::unique_ptr<Layer>& first, std::unique_ptr<Layer>& second)
{
	std::cout << "connecting matrix to matrix: ";
	int counter = 0;
	std::cout << counter << " connections made" << std::endl;
}
